<view class="container">
    <map longitude="{{longitude}}" latitude="{{latitude}}" id="ofoMap" scale="{{scale}}"
    controls="{{controls}}"></map>  
</view> 
    <!-- UI组件 一组标签？map 是原生的html提供不了的
    html标签在诞生时，展示文档，简单就好，能力有限
    我提供一个牛逼的功能，封装起来，用一个新的标签来表示
    功能极其强大 -->
